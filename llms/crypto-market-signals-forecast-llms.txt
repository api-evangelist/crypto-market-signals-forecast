# x402.coinopai.com — Kronos Intelligence API
> Auditable market context with calibrated forecast ranges, risk state, decision journaling, and post-decision verification. Pay per call via x402 on Base mainnet. No API key needed.

## Kronos Market Intelligence (live, 15-min cadence)
Kronos is market intelligence only. It gives agents calibrated ranges, risk_state, directional_bias, review_level, response_schema_version, compliance_mode=market_intelligence_only, generated_for=research_and_decision_support, and a decision_id for outcome audit.
GET /api/kronos/signals — $0.05 — Market context, freshness, and score details
GET /api/kronos/history — $0.05 — Recent context history for analysis
GET /api/kronos/preflight — $0.05 — Market-state preflight with risk_state, review_level, directional_bias, and observation_thresholds
GET /api/kronos/decision — $0.15 — Market-intelligence journal with directional_bias, anomaly scoring, calibration, verification, liability disclaimers, and decision_id. No output is a market activity instruction.
GET /api/kronos/audit — $0.07 — Audit market-intelligence outcome vs real prices
GET /api/kronos/forecast — $0.05 — Conformally-calibrated 80% price range (~0.80 empirical coverage)
GET /api/kronos/risk — $0.02 — Current market risk state and cooldown context

## ForgeMesh Signal Safety
GET /api/catalog — $0.005 — Catalog of anomaly, calibration, and verification products
POST /api/anomaly — $0.07 — Market signal anomaly review. Returns anomaly_score, anomaly_level, review_label, drivers, agreement_score, sample_size, and liability disclaimers. Market intelligence only; no output is an instruction to trade, transact, hold, rebalance, or take market activity. legacy_fields.recommended_action is compatibility only.
POST /api/calibration — $0.05 — Forecast confidence calibration. Returns raw_confidence, calibrated_confidence, confidence_band, historical_hit_rate, sample_size, calibration_grade, calibration_data_status, directional_bias, and liability disclaimers. Prefer directional_bias; signal_type is a legacy directional-context alias, not asset class. Market intelligence only; no output is an instruction to trade, transact, hold, rebalance, or take market activity.
POST /api/verify — $0.05 — Preflight review for autonomous decision-support workflows. Prefer directional_bias; proposed_action is a legacy directional-context alias.

## Status
Live: market context, risk assessment, decision journaling, outcome verification, calibrated price ranges (forecast), anomaly scoring, confidence calibration, and rule verification.
Note: directional_bias alone has not been validated as a standalone market-timing edge in backtest (~51% accurate); the calibrated 80% range is the validated product. Forecast-vs-outcome agreement analysis is still collecting evidence and is not exposed publicly.

## ForgeMesh Service Catalog
GET /api/categories — $0.005 — Product categories: market_intelligence, workflows, media_generation, infrastructure
GET /api/services — $0.005 — Purchasable service cards with pricing, descriptions, and response highlights
GET /api/services?category=market_intelligence — $0.005 — Filter by category

## Workflows (819 prompts, 35 categories, 115 platforms)
GET /api/search?q={query} — $0.01 — Search automation prompts
GET /api/automation/:slug — $0.01 — Get full workflow prompt

## Payment
- Network: Base mainnet (eip155:8453)
- Receiving wallet: 0x1304EC1A8945365e43A5c18a734065f107B417cA
- Facilitator: https://x402.xyz/facilitate
- Live extensions: Bazaar discovery and optional payment-identifier idempotency
- Signed offer/receipt extension: enabled
- Payment identifier extension: live
- Builder code extension: configured_when_builder_code_present
- Sign-in-with-x: not_needed_for_one_shot_paid_outputs

## ForgeMesh Fleet
This service is one of 11 x402 services and counting from ForgeMesh Labs, 500+ paid endpoints, from $0.001 — one wallet, no signup, atomic USDC settlement on Base.
- Paid registry: POST https://x402.forgemesh.io/fleet-products ($0.05)
- Paid product detail: POST https://x402.forgemesh.io/fleet-product-detail ($0.05)
- Free index: https://x402.forgemesh.io/llms.txt